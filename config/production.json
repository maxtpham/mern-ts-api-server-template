{
    // UNSUED: "Host": "localhost",         // server binding host (default to localhost)
    // UNSUED: "Port": 3000,                // server listen port (if n/a then no server wil be created)
    "MongoDB": "mongodb://localhost/test"
}